#include "lists.h"
/**
 * get_nodeint_at_intdex - returns the th node of a listint_t linked list.
 * @head: pointer  to the first node in the list
 * @index: index of the node to return
 * Return: pointer to the indexed node
 */
