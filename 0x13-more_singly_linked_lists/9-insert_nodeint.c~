#include "lists.h"
#include <stdlib.h>
/**
 * insert_nodeint_at-index - inserts a listint_t node at index in list
 * @head: head of list
 * @idx: index to add node at, starting at 0
 * @n: value of node to add
 * Return: new node address if success, else NULL
 */
listint_t *insert_nodeint_at_index(listint_t **head, unsigned int idx, int n)
{
  
}
