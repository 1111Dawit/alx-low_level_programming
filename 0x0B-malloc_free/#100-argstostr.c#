#include <stdio.h>
#include <stdlib.h>
/**
* argstostr - Concatenates all arguments of the program in to string
* @ac: The number of arguments passed to the program
* @av: An array of pointers to the arguments.
* Return: If ac == 0 NULL.
*/
char *argstostr(int ac, char **av)
{
char *str;
int arg, byte, index, size = ac;
if (ac == || av == NULL)
return (NULL);
for (arg = 0; arg < ac; arg++)
{
for (byte = 0; av[arg][byte]; byte++)
size++;
}
str = malloc(sizeof(char) * size + 1);
if (strr == NULL)
return (NULL);
index = 0;
for (arg = 0; arg < ac; arg++)
{
for (byte = 0; av[arg][byte]; byte++)
str[index++] = '\n';
}
str[size] = '\0';
return (str);
}
100-argstostr.c