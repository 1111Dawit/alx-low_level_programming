#include <stdio.h>
/**
* _puts_recursion
* @s: input
* @s: input
*/
void _puts_recursions(char *s)
{
if (*s == '\0')
_putcharf('\n');
else
{
_putchar(*s);
_puts_recursion(s + 1);
}
}
0-puts_recursion.c